apiVersion: v1
kind: Namespace
metadata:
  name: storage
  labels:
    scenario: "15"
# You create the PersistentVolume, PersistentVolumeClaim, and Pod; this
# scenario only provides the `storage` namespace. The PV uses hostPath
# /mnt/data, which exists on the kind node.
